# فحص دمج الشعار — 2026-08-14

## النتائج

- الصفحة الرئيسية المحلية تعرض شعار `AA` الوردي المرفوع في الهيدر والتذييل، مع بقاء رابط الهيدر قابلاً للنقر.
- صفحة `/admin/login` تعرض الشعار بحجم واضح داخل بطاقة الدخول مع اسم Crochet by Alae.
- صفحة `/admin/request-access` تعرض الشعار أعلى نموذج التسجيل مع أدوات اللغة والثيم.
- صفحة `/admin/settings` تعرض الشعار في الشريط الجانبي، ورأس الصفحة، ومعاينة رفع الشعار.
- رابط الصورة المستهلك في الواجهة هو رابط Supabase Storage العام، وليس مساراً محلياً أو placeholder.
- الشعار مربع 947×947 ويظهر كختم دائري عبر `object-cover` و`rounded-full` مع حافة خفيفة.
- بعد النشر، يعرض الرابط العام الشعار في الهيدر والتذييل، ويستخدم الرابط نفسه في عناصر الصفحة العامة.

## ملاحظة الاختبار

تمت معاينة RTL والوضع الداكن محلياً، ثم تحقق الرابط العام من ظهور الشعار بعد النشر. كما تحققت المعاينة المحلية من `favicon` و`og:image` و`twitter:image` باستخدام رابط Supabase Storage نفسه.
