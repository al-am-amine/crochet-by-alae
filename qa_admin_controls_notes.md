# QA notes — Admin controls

## 2026-08-14

The latest local preview was opened at `/admin/login` on port 4176. The shared admin controls are visible in the top corner, with the language button and theme button exposed as accessible buttons with translated labels.

The language toggle was clicked once and successfully changed the page from Arabic to English, including the login labels and the control hints. The control cluster moved with the document direction as expected and remained visible without covering the form.
