# ملاحظات جلسة Supabase

بتاريخ 13 أغسطس 2026، فُتحت لوحة مشروع Crochet by Alae على الرابط:
https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/auth/users

الجلسة المالكة متاحة، وظهرت صفحة Authentication → Users. أظهرت الصفحة الحساب `admin.crochetbyalae@gmail.com` ضمن المستخدمين، مع تقدير إجمالي 10 مستخدمين. تم فتح محرر SQL داخل الصفحة، ولم تُنفذ أي عملية قاعدة بيانات في هذه الملاحظة.

## متابعة التنفيذ

تم فتح SQL Editor في المسار `/dashboard/project/ikgxaxanhxgnsadkawuz/editor`، ثم أُعيد تشغيل المخطط المصحح بنجاح بعد معالجة خطأ صياغة `is_super_admin_user()`. تم فتح جدول `admin_users` للتحقق، ثم عادت الجلسة إلى Authentication → Users. ظهر الحساب القديم ضمن Authentication؛ إنشاء الحساب الإداري الجديد ما زال في نموذج Supabase المفتوح. لا تحتوي هذه الملاحظات على كلمات مرور أو مفاتيح سرية.

تم إنشاء المستخدم `m.amine.amttout@gmail.com` بنجاح من نموذج Supabase Authentication مع تفعيل التأكيد التلقائي. أظهرت الصفحة رسالة نجاح، وظهر الحساب الجديد بجانب الحساب القديم في قائمة Authentication. لم تُحفظ كلمة المرور في المشروع أو في هذه الملاحظات.

تم تشغيل استعلام قراءة على `public.admin_users`، وكانت النتيجة صفاً واحداً فقط: الحساب الجديد بدور `super_admin` وحالة `is_active = true`، مع صلاحيات الإدارة الكاملة. لم يظهر الحساب القديم في جدول صلاحيات الأدمن. هذا يؤكد عزل صلاحيات لوحة الأدمن، مع بقاء هوية الحساب القديم في Authentication فقط.

عند الانتقال لاحقاً إلى صفحة Edge Functions أعادت Supabase توجيه الجلسة إلى صفحة تسجيل الدخول، لذلك لم تُنشر الدالة الخادمية بعد. يلزم تسجيل الدخول مجدداً داخل الجلسة المتصلة قبل متابعة النشر.

تم اختيار Continue with GitHub بناءً على طلب المستخدمة، لكن Supabase أعاد الصفحة إلى تسجيل الدخول وظهر تحقق hCaptcha؛ لم تكتمل جلسة Edge Functions ولم تُنفذ أي عملية نشر للدالة.

عادت الجلسة للعمل في 13 أغسطس 2026، وظهرت صفحة Edge Functions على الرابط:
https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/functions
وتوفر Supabase النشر عبر Via Editor أو Via CLI أو AI Assistant. سيتم استخدام Via Editor لأن مصدر الدالة موجود محلياً ولا توجد جلسة CLI معتمدة.

فتح محرر الدالة عبر:
https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/functions/new
وتم تثبيت اسم الدالة `admin-user-management` في حقل Function name. الكود المحلي الجاهز للرفع موجود في `supabase/functions/admin-user-management/index.ts`.

تم إدخال الكود الكامل في نموذج Monaco داخل محرر Supabase بنجاح، بطول 6180 حرفاً، مع بقاء Service Role Key معتمداً على أسرار Edge Function فقط.

ظهر اسم الدالة `admin-user-management` في النموذج، وتم تشغيل زر Deploy function مباشرة من الواجهة بعد التحقق من أن الزر غير معطل. يلزم فحص رسالة النتيجة أو نافذة التأكيد التالية.

تم نشر الدالة بنجاح وظهرت صفحة التفاصيل:
https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/functions/admin-user-management/details
Endpoint:
https://ikgxaxanhxgnsadkawuz.supabase.co/functions/v1/admin-user-management
صفحة التفاصيل تعرض خيار Verify JWT with legacy secret، مع توصية Supabase بإيقافه عند استخدام تحقق مخصص داخل الدالة؛ الدالة الحالية تتحقق من Bearer token عبر `auth.getUser()` ومن دور Super Admin داخل `admin_users`.

فحص DOM أظهر أن Verify JWT مضبوط على `checked=true`. محاولة النقر البرمجي لم تغيّر الحالة وبقي زر Save changes معطلاً، لذلك ستتم محاولة التبديل عبر عنصر الواجهة المرئي قبل اختبار endpoint.

بعد النقر المرئي بقيت الحالة `checked=true` وزر Save changes معطلاً؛ لم يتم تغيير إعداد Verify JWT. الدالة منشورة ويمكن الانتقال إلى اختبارها، مع ملاحظة أن كودها يطبق تحقق `auth.getUser()` وصلاحية Super Admin بنفسه.

## فحص جلسة 14 أغسطس 2026

لوحة المشروع متاحة في جلسة المالك الحالية، وحالة المشروع **Healthy** على الخطة المجانية. المشروع على فرع الإنتاج `main` ولا يوجد مستودع GitHub متصل من إعدادات Supabase. محرر SQL متاح، ولم يُنفَّذ أي استعلام أو نشر جديد في هذه الجلسة بعد تعديل دورة طلبات الأدمن محلياً.

في متابعة 14 أغسطس 2026 عادت جلسة المالك إلى محرر SQL على الرابط `/sql/68d8e51f-5152-4247-8d9b-2da46fe6ba8f`. تم اختبار الإدخال بنص قصير فقط ثم استبداله، ولم يُنفَّذ استعلام تغييري بعد. المخطط المحلي النهائي يضيف `user_id` و`additional_info` وRPC `get_my_request_status()` ويزيل عقد الصلاحيات القديم؛ لا تحتوي هذه الملاحظة على كلمات مرور أو مفاتيح سرية.

تم إدخال الجزء الأول من المخطط ثم الجزء الثاني الذي ينشئ `submit_admin_access_request` بالمعاملات الجديدة. محرر SQL ما زال في وضع `Unsaved edits` ولم يتم الضغط على Run؛ يلزم إكمال RPC الحالة وسياسة Super Admin ثم مراجعة النص قبل التنفيذ.

اكتمل إدخال RPC `get_my_request_status()` وسياسة قراءة `admin_users` المقيدة بـ `is_super_admin_user()`. المخطط الكامل موجود الآن في المحرر، ولم يُنفَّذ بعد؛ ستتم مراجعته ثم تشغيله بعد التأكيد السابق.

أظهر فحص DOM أن محرر Monaco يعرض الأسطر بصرياً على طبقات وأن حقل `textarea` المخفي لا يمثل النص الكامل؛ لذلك لا يُعتمد على طول هذا الحقل للتحقق. لم يُضغط Run ولم تُنفذ تغييرات قاعدة البيانات حتى الآن.

تم اكتشاف نموذج Monaco الحقيقي عبر `window.monaco.editor.getModels()`، وطوله الحالي غير موثوق بسبب الإدخال المتقطع. سيُستبدل مباشرة بالمحتوى الحرفي للملف المحلي `supabase_admin_access_requests.sql`، ثم ستتم مراجعة النص قبل التشغيل.

تم استبدال نموذج Monaco مباشرة بالمخطط المحلي الكامل بطول 7199 حرفاً و188 سطراً. بعد تأكيد المستخدم، تم الضغط على Run ثم تأكيد التحذير؛ محرر Supabase يعرض الآن `Running...`، ولم تظهر النتيجة النهائية بعد.

اكتمل التنفيذ لاحقاً وظهرت نتيجة Supabase: `Success. No rows returned`. تم تطبيق الجدول، الحقول، الفهارس، السياسات، وRPCs الخاصة بالتسجيل وحالة الطلب بنجاح. فحص CLI المحلي أظهر أن أمر `supabase` غير مثبت، لذلك سيُستخدم مسار نشر Edge Function عبر لوحة Supabase أو إعداد CLI مؤقت موثوق.

## متابعة نشر Edge Function — 14 أغسطس 2026

تم إدخال كود `admin-user-management` المحدث في محرر Supabase بعد تأكيد المستخدم. ظهرت استجابات HTTP ناجحة `200` و`201` في لوحة السجلات عقب الضغط على `Deploy updates`، لكن يلزم فتح صفحة التفاصيل أو السجلات للتأكد من أن الإصدار الجديد أصبح منشوراً فعلاً قبل اعتباره نهائياً. لا تحتوي هذه الملاحظة على كلمات مرور أو مفاتيح سرية.

في صفحة الإعدادات بتاريخ 14 أغسطس 2026، يظهر خيار `Verify JWT with legacy secret` موثقاً على أنه يتطلب JWT موقعاً بالمفتاح القديم، مع توصية Supabase بإيقافه عند استخدام تحقق مخصص داخل الدالة. بعد تأكيد المستخدم، تم النقر على مفتاح التبديل؛ يجب حفظ التغيير عبر `Save changes` ثم إعادة اختبار endpoint العام. المصدر: https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/functions/admin-user-management/details

بعد حفظ الإعداد، أعاد endpoint حالة `401 Authentication required` عند إرسال طلب تشخيصي غير مصادق. أُعيد الاختبار بطلب `submit_request` ببيانات غير صالحة لا تنشئ سجلاً، وبالمفتاح العام المستخرج من `src/lib/supabaseClient.js`، فبقيت النتيجة `401`. هذا يشير إلى أن النسخة الحية لا تزال تفرض المصادقة قبل فرع التسجيل العام، أو أن Deploy status غير متاح ولم يتزامن الإصدار الجديد؛ يلزم التحقق من نسخة الدالة الحية قبل اعتبار التسجيل العام فعالاً.

عند إعادة فتح إعدادات الدالة، ظهر مفتاح Verify JWT واحداً، لكن قراءة DOM لم تعرض `aria-checked` أو `data-state`، وكان زر `Save changes` معطلاً. لذلك لا يمكن الجزم من الواجهة بأن الإعداد المحفوظ أصبح OFF؛ نتيجة `401` تتوافق مع بقاء التحقق البوّابي أو مع عدم مزامنة إعداد النشر.

محاولة النقر باستخدام فهرس قديم من صفحة الإعدادات أعادت الجلسة إلى صفحة `Edge Function Secrets` بدلاً من تبديل المفتاح. الصفحة تعرض الأسرار الافتراضية فقط، ولا توجد أسرار مخصصة، وتؤكد أن `SUPABASE_ANON_KEY` و`SUPABASE_SERVICE_ROLE_KEY` قديمان ومهجوران لصالح مفاتيح publishable/secret الجديدة. لم تُقرأ قيم الأسرار ولم تُعدّل.

بعد العودة إلى صفحة الإعدادات والنقر على المفتاح الحالي، أصبح زر `Save changes` مفعلاً وقراءة DOM أعادت `aria-checked: true` و`data-state: checked`. بما أن الهدف هو OFF، يجب تبديله مرة أخرى إلى `false` قبل الحفظ.

بعد التبديل الثاني، أعادت واجهة Supabase `aria-checked: false` و`data-state: unchecked`، وكان زر `Save changes` معطلاً. هذا يعني أن إعداد Verify JWT ظاهر حالياً كـ OFF ومحفوظ، لذلك أُعيد اختبار endpoint بعد هذه الحالة بدلاً من الضغط على زر غير متاح.

مصدر التحقق الخارجي: `https://supabase.com/dashboard/project/ikgxaxanhxgnsadkawuz/functions/admin-user-management/code`. بعد فتح صفحة Code، ظهرت عبارة `Deploy status unavailable` وبقي محرر الكود فارغاً مع مؤشر تحميل، لذلك لا يمكن من هذه الصفحة إثبات أن النسخة المحدثة حُفظت أو نُشرت. هذا يفسر استمرار 401 ويستدعي استخدام مسار نشر موثوق بدلاً من اعتبار النشر ناجحاً.

في فحص لوحة Supabase اللاحق، أعاد المسار الداخلي `/dashboard/api/get-deployment-commit` استجابة 200 مع commit عام للوحة (`67d4fed40dc489c3a9d65f0df5d164be64cdaded`) ووقت 2026-08-14T04:29:39Z، لكنه لا يثبت إصدار Edge Function نفسه. ما زال يلزم مسار نشر موثوق أو تحقق مباشر من سجل الدالة قبل اعتبار تسجيل الأدمن العام فعالاً.

بعد عودة جلسة Supabase، ظهرت صفحة Code للدالة وبها زر `Deploy updates`، لكن النص الفعلي في نموذج Monaco طوله 6715 حرفاً ولا يحتوي `submit_request`، ويحتوي `inviteUserByEmail` والتعليق القديم الخاص بالدعوات. هذا يثبت أن النسخة الموجودة في محرر Supabase ما زالت العقد القديم، وأن محاولات النشر السابقة لم تجعل النسخة الجديدة حية. يجب إعادة إدخال الملف المحلي الجديد في محرر Monaco ثم نشره والتحقق من النص الحي قبل اختبار التسجيل العام.
