# ملاحظات فحص إصلاحات الواجهة — 2026-08-14

فتحت صفحة `/admin/settings` المحلية بعد تسجيل دخول Super Admin. البناء الإنتاجي نجح، لكن المعاينة الحالية تعرض رمزاً قديماً على صف WhatsApp ولا تعرض عناصر `aria-label` الخاصة بـChannelIcon أو عناصر `role="switch"` المتوقعة من التعديل الأخير. بعد إعادة تشغيل Vite وإعادة التحميل بعنوان مؤقت، أكد DOM وجود صفر من عناصر `role="switch"` وصفر من عناصر SVG، مع بقاء رموز Material القديمة؛ لذلك يجب تحديد الجذر الفعلي للنسخة أو مسار الاستيراد قبل اعتبار الفحص المرئي ناجحاً.

بعد تحديد أن `App.jsx` يستورد من `src/pages/admin` لا من `src/admin`، طُبّقت الإصلاحات على الملفات المستخدمة فعلياً. بعد إعادة تشغيل خادم latest، ظهرت أيقونة WhatsApp المرسومة داخل خلفية شفافة، وظهر DOM بثلاثة مفاتيح `role="switch"` وحالات `aria-checked="true"`، كما بقيت الدائرة داخل إطار المفتاح بصرياً.

اختُبرت العربية RTL والوضع الداكن من صفحة الإعدادات. انعكس ترتيب الأعمدة والمحتوى بشكل صحيح، وبقيت أيقونة WhatsApp واضحة، كما ظلّت دائرة المفتاح ضمن مساره مع تباين مقبول للنصوص والحدود في الوضع الداكن.

في صفحة سجل الأمان، ظهرت نافذة التأكيد بشكل صحيح. النقر المفهرس لم يغلقها، ثم أُغلقت بالنقر المباشر على زر التأكيد؛ لكن نتيجة المتصفح التالية ما زالت تعرض صفوف السجل، لذلك لا يُعدّ التفريغ ناجحاً حتى يتم التحقق من الرسالة والبيانات من جديد.

بعد إغلاق النافذة لم يظهر خطأ JavaScript واضح في سجل المتصفح، لكن إعادة عرض الصفحة ما زالت تُظهر السجلات القديمة؛ يلزم فحص استجابة RPC نفسها بدلاً من اعتبار العملية ناجحة.

استدعاء RPC من جلسة Super Admin أعاد HTTP 400 برسالة PostgreSQL: `DELETE requires a WHERE clause`. الجلسة والحارس الأمني يعملان، لكن تعريف الدالة يحتاج شرط حذف صريحاً مثل `WHERE id IS NOT NULL` قبل إعادة التنفيذ.

بعد تطبيق ترحيل الإصلاح، أعاد RPC المحمي HTTP 200 والنتيجة `54`، أي تم حذف 54 سجلاً فعلياً من سجل الأمان. لم يُستخدم حذف مباشر خارج الحارس؛ التنفيذ تم من جلسة Super Admin الموثقة.
