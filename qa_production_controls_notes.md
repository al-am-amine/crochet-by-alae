# QA notes — Published admin controls

## 2026-08-14

On the ready immutable Netlify deployment, the admin login language control switched the page from Arabic to English and translated the labels and helper copy. The theme control then switched the page from light to dark while keeping the form, labels, buttons, and footer readable. Both controls remained visible in the correct corner after the direction change.
