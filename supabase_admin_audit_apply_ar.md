# تطبيق سجل أمان الأدمن في Supabase

يضيف الملف `supabase_admin_audit.sql` جدولاً خاصاً بمحاولات الدخول إلى لوحة الأدمن وأنشطة الأدمن فقط. لا يضيف أي تتبع لتصفح المتجر العام أو للزبائن.

## طريقة التطبيق

افتحي مشروع Supabase الخاص بـ Crochet by Alae، ثم اذهبي إلى **SQL Editor** وأنشئي استعلاماً جديداً. انسخي محتوى `supabase_admin_audit.sql` كاملاً، ثم راجعيه واضغطي **Run** مرة واحدة. يمكن إعادة تشغيله بأمان نسبياً لأنه يستخدم `create if not exists` و`create or replace` و`drop policy if exists` للأجزاء الخاصة بالسجل.

بعد التنفيذ، سجّلي خروجاً ثم ادخلي بحساب الأدمن، وافتحي `/admin/security-log`. يجب أن يظهر حدث الدخول الناجح. جرّبي أيضاً كلمة مرور غير صحيحة مرة واحدة للتأكد من تسجيل محاولة الفشل، ثم أعيدي الدخول الصحيح. عند تكرار ثلاث محاولات فاشلة في جلسة المتصفح نفسها يظهر تنبيه محلي في صفحة تسجيل الدخول.

## حماية بيانات الدخول

بما أن كلمة مرور الأدمن كُتبت في محادثة، يجب تغييرها أولاً من **Supabase Dashboard → Authentication → Users**، ثم تفعيل عامل ثانٍ مثل MFA أو Passkey من إعدادات المصادقة. لا تضعي كلمة المرور الجديدة في ملفات المشروع أو GitHub أو متغيرات الواجهة. المفتاح المسموح للواجهة هو مفتاح Supabase العام `anon` فقط، أما `service_role` وأي أسرار خاصة فيجب أن تبقى داخل خدمة خادمية أو Edge Function.

## حدود السجل

السجل يعرض البريد المرتبط بمحاولة الدخول، وقت الحدث، نوع الإجراء، ومعرّفات الموارد اللازمة لفهم تعديل الأدمن. لا تُسجّل قيم قنوات التواصل أو بيانات الطلبات الشخصية في تفاصيل النشاط، ولا توجد أي دالة أو جدول جديد لتسجيل زوار الموقع العام.
