# نتائج فحص حذف الطلبات والمنتجات

تاريخ الفحص: 2026-08-14

المشروع المتصل: `ikgxaxanhxgnsadkawuz`

## النتائج المؤكدة

سياسة جدول `orders` الحالية تسمح للمستخدم المصادق عليه بالقراءة والتحديث والإدراج العام فقط، ولا توجد سياسة `DELETE`. لذلك يفشل حذف الطلبات من واجهة الأدمن حتى مع وجود جلسة مصادقة صحيحة.

سياسة جدول `products` الحالية تحتوي على `ALL` للمستخدم المصادق عليه، لكن جدول `orders` يملك مفتاحاً أجنبياً إلى `products(id)` من دون `ON DELETE SET NULL` أو `ON DELETE CASCADE`. لذلك قد يفشل حذف منتج مرتبط بطلبات موجودة بسبب قيد المفتاح الأجنبي. يجب الحفاظ على سجل الطلبات، ولذلك يكون الحل الآمن هو فصل `orders.product_id` إلى `NULL` قبل حذف المنتج، مع إبقاء `product_name` التاريخي.

## ملاحظات التنفيذ

ينبغي أن يعرض التطبيق خطأ Supabase بدلاً من تجاهله، وأن يستخدم تأكيداً صريحاً قبل الحذف، ثم يعيد تحميل القائمة بعد نجاح العملية ويسجل النشاط للأدمن. لا يجوز إدخال بيانات تجريبية أو حذف سجل غير محدد من المستخدم.
