# سجل الفحص الخاص قبل الفتح العام

تاريخ الفحص: 2026-08-14

## حالة Netlify

تم إنشاء موقع Netlify باسم `crochetbyalae` بالحساب الجديد، ورفع النسخة المبنية من مجلد `dist`. الاستجابة الخارجية للرابط `https://crochetbyalae.netlify.app/` أعادت HTTP 401، لذلك لا يظهر الموقع للعامة حالياً. ستبقى حماية الوصول مفعلة حتى نهاية الفحص.

عند فتح الرابط في المتصفح، حوّل Netlify الطلب إلى بوابة الحماية `https://app.netlify.com/edge-access?domain=crochetbyalae.netlify.app&requested_path=%2F&site_id=b45d85ed-c32b-4ad9-b118-362ccb728447`، وهو ما يؤكد أن الموقع خلف Edge Access وليس متاحاً للعامة أثناء الفحص.

بعد إزالة حماية الوصول بناءً على تأكيد المستخدم، فتح الرابط العام `/custom-design` لكنه أعاد صفحة Netlify `Page not found`. هذا يكشف حاجة نسخة SPA إلى قاعدة fallback في Netlify (`/* /index.html 200`) للمسارات المباشرة؛ يجب إصلاحها وإعادة النشر قبل اعتبار الموقع العام سليماً.

## البناء والمسارات المحلية

نجح `npm run build` باستخدام Vite. أعادت المعاينة المحلية HTTP 200 للمسارات `/` و`/shop` و`/faq` و`/admin/login` و`/admin/request-access`.

## ملاحظات الواجهة الأولية

ظهرت الصفحة الرئيسية بالعربية مع روابط الرئيسية والمتجر والطلب الخاص وعن العلامة، ومفتاح اللغة EN، وزر السلة، ومفتاح الوضع الداكن. ظهرت رسالة عدم وجود منتجات في المعاينة، وهو empty state وليس خطأً بحد ذاته إذا لم توجد منتجات فعلية في قاعدة البيانات. ظهر تنبيه من Vite عن حجم حزمة JavaScript أكبر من 500 kB بعد الضغط؛ هو تحذير أداء وليس فشل بناء، وسيُعالج فقط إذا ظهر أثر فعلي في الاختبار.

أعاد المسار `/shop` HTTP 200 وظهر empty state مع رابط الطلب الخاص. أعاد المسار `/faq` HTTP 200 وظهر الأكوردين؛ السؤال الأول مفتوح افتراضياً والأسئلة الأخرى قابلة للفتح، مع ظهور رابط سياسة التوصيل.

أعاد المسار `/admin/login` HTTP 200 وظهر نموذج البريد وكلمة المرور وزر إظهار كلمة المرور ورابط طلب الوصول. أعاد المسار `/admin/request-access` HTTP 200 وظهر نموذج صفحة واحدة يحتوي الاسم واللقب والبريد وكلمة المرور ومعلومات إضافية اختيارية، مع تنبيه بعدم إدخال كلمات المرور أو رموز التحقق في المعلومات الإضافية، وعدم منح الصلاحية قبل موافقة المالك. لم يتم إرسال أي بيانات حقيقية.

اختبار HTTP المجمع للمسارات `/`, `/shop`, `/product/missing`, `/custom-design`, `/cart`, `/about`, `/shipping`, `/faq`, `/admin/login`, `/admin/request-access`, `/admin`, `/admin/products`, و`/admin/admin-requests` أعاد HTTP 200 لكل مسار في المعاينة المحلية.

بعد تعديل `CustomRequest.jsx` لإضافة مساحة علوية متوافقة مع شريط التنقل الثابت (`pt-28` للهاتف و`pt-32` للشاشات الأكبر) وتحسين التفاف العنوان، تم فتح الصفحة محلياً وتبديلها إلى العربية. ظهر العنوان `اطلبي تصميمك الخاص` أسفل شريط التنقل وفي حاويته دون تداخل أو قص ظاهر، مع بقاء ترتيب النموذج والهوية البصرية كما هي.

بعد فتح الموقع للعامة، ظهر أن المسار المباشر `/custom-design` يعيد 404 لأن ملف `_redirects` كان خارج `dist`. أُضيف `public/_redirects`، ثم نجح البناء والنشر الجديد، وأصبح Netlify يعالج قاعدة redirect واحدة. تحقق HTTP النهائي أعاد 200 للمسارات `/` و`/custom-design` و`/shop` و`/admin/login`، مع ظهور عنوان الموقع `Crochet by Alae`.

## سياسة الإصلاح

سيتم جمع الأخطاء المؤكدة فقط في قائمة واحدة، ثم تطبيق الإصلاحات الضرورية في تمريرة واحدة، وإعادة البناء والنشر الخاص مرة واحدة. لن تُزال حماية الوصول ولن يُحذف أي مشروع أو حساب قديم قبل موافقة المستخدم الصريحة.

## تحسين الوضع الليلي ونموذج طلب الأدمن

في المعاينة المحلية، كان الوضع الليلي مفعلاً، وأصبح النص الأساسي بلون فاتح مقروء (`rgb(255, 250, 248)`) والخلفية الداخلية للحقول داكنة (`rgb(36, 36, 36)`). أصبح حقل المعلومات الإضافية بارتفاع `48px`، قريباً من حقل كلمة المرور البالغ `46px`، مع بقاء التنبيه الأمني ظاهراً وقابلاً للتعديل من ملف `src/i18n/translations.js`. لم تُرسل أي بيانات أو كلمة مرور حقيقية أثناء الاختبار.

## فحص صفحة مراجعة الطلبات المنشورة

عند فتح `/admin/admin-requests` من دون جلسة أدمن صالحة، أعاد التطبيق التوجيه إلى `/admin/login` وأظهر نموذج تسجيل الدخول فقط. هذا يؤكد أن حراسة المسار تعمل، ولم تُنفذ عملية قبول أو رفض حقيقية أثناء الاختبار. يلزم اختبار المراجعة بعد تسجيل دخول Super Admin فعلياً.
