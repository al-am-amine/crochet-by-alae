# QA notes — Public storefront smoke check

## 2026-08-14

The local preview loaded both `/` and `/shop` without runtime errors. The public header still exposes the original Crochet by Alae wordmark, navigation, language button, cart link, and theme button. The homepage continues to state delivery in Blida and Algiers, and the shop route preserves its empty-state and custom-order escape route.
