# نتائج التحقق المحلي

بتاريخ 2026-08-12، تم تشغيل نسخة العمل الأصلية على خادم Vite محلي مؤقت.

| المسار | النتيجة |
|---|---|
| `/` | تحمّلت الصفحة الرئيسية بعنوان Crochet by Alae، وظهرت اللغة العربية ومناطق التوصيل والروابط العامة دون خطأ ظاهر. |
| `/admin` | تم التحويل تلقائياً إلى `/admin/login` عند عدم وجود جلسة مصادقة، ولم تظهر لوحة الأدمن أو سجلها للزائر غير المسجل. |

المعاينة البصرية أظهرت أن بطاقة تسجيل الدخول حافظت على ألوان وتصميم الهوية الحالية. لم تُستخدم أي كاميرا أو آلية لتتبع زوار الموقع العام في التغييرات الجديدة.

فحص وحدة تحكم المتصفح لم يُظهر أخطاء JavaScript؛ ظهرت فقط تحذيرات مستقبلية اعتيادية من React Router حول `startTransition` و`splat paths`.

| المسار | النتيجة |
|---|---|
| `/shop` | تحمّلت صفحة المتجر مع زر الفلترة والحالة الفارغة المتوقعة محلياً عند عدم وجود منتجات محلية، دون خطأ في واجهة المسار. |
| `/faq` | ظهرت الأسئلة الأربعة ومعلومة التوصيل إلى البليدة والجزائر العاصمة، مع بقاء الروابط العامة سليمة. |

## حالة تفعيل سجل الأدمن أونلاين

محرر Supabase مفتوح على مشروع `crochet-by-alae` الصحيح. حدث تشوّه في الإدخال الأول بسبب اقتراحات محرر Monaco، ثم استُبدل نموذج Monaco مباشرةً بنسخة SQL سليمة. تم تشغيل المخطط المختصر في Supabase بنجاح بتاريخ 2026-08-12، وظهرت النتيجة `Success. No rows returned`. أصبح جدول `admin_audit_log` ودوال الحماية وسياسات RLS مفعّلة أونلاين. لا يحتوي الاستعلام على كلمات مرور أو صور أو تتبع لزوار المتجر العام.

استعلام قراءة لاحق في محرر Supabase أكد أن `admin_audit_log` موجود، وأن الدوال `admin_login_gate` و`log_admin_login_attempt` و`log_admin_page_attempt` و`log_admin_action` موجودة جميعاً، وكل قيم التحقق عادت `true`.

## تحقق الموقع الرئيسي بعد النشر

تم فتح `https://crochetbyalae.netlify.app/` بعد نشر Netlify رقم `6a7ce635e1840b778fb03f10`، وظهرت الصفحة الرئيسية بعنوان `Crochet by Alae`، ومعلومة التوصيل إلى البليدة والجزائر العاصمة، وروابط المتجر وقصة المشروع. لم يظهر البريد أو كلمة المرور أو طلب كاميرا في الصفحة العامة.

عند فتح `/admin` في جلسة المتصفح الحالية المصادق عليها مسبقاً، ظهرت لوحة المدير ورابط `سجل أمان الأدمن` وسنة 2026. هذه النتيجة تثبت عمل الجلسة المصادق عليها، ولا تثبت سلوك الزائر غير المسجل؛ يجب تنفيذ اختبار تسجيل الخروج ثم إعادة فتح `/admin` للتحقق من التحويل إلى صفحة الدخول.

بعد الضغط على تسجيل الخروج ثم إعادة فتح `https://crochetbyalae.netlify.app/admin`، تم التحويل فعلياً إلى `/admin/login`. ظهرت صفحة تسجيل دخول الأدمن مع نص يوضح أن التسجيل يخص محاولات الأدمن ومؤشرات تقنية محدودة للحماية، وظهر التذييل بسنة 2026. لم يظهر للزائر غير المصادق أي محتوى من لوحة التحكم.

في الجولة المرئية الأخيرة على `/shop`، انتهى التحميل وظهرت حالة «لا توجد منتجات بعد» لأن قاعدة البيانات لا تحتوي منتجات حالياً، مع ظهور شريط التوصيل وحقوق النشر 2026. لم تظهر حلقة تحميل مستمرة أو رسالة خطأ واجهة في هذه الحالة.

في الجولة التالية، ظهرت `/cart` بحالة السلة الفارغة مع رابط واضح للعودة إلى المتجر وتذييل 2026. ظهرت `/custom-design` بتخطيط ثنائي العمود، حقول الوصف والاسم والهاتف، ورفع صورة اختياري؛ لم يتم إرسال أي طلب حقيقي أثناء الاختبار.

تم تبديل `/custom-design` إلى الإنجليزية ثم إلى الوضع الداكن. ظهرت النصوص الإنجليزية واتجاه التخطيط الصحيح، وبقيت حقول النموذج قابلة للقراءة، مع ظهور زر `light_mode` للعودة للوضع الفاتح. لم يظهر خطأ JavaScript أو انكسار واضح في التخطيط.

بعد إصلاح تباين الوضع الداكن في النصوص التعريفية وعبارة رفع الصورة، أعيد فتح `/custom-design` من آخر build. في الوضع الداكن أصبح النص التعريفي أبيضاً بدرجة مقروءة، وبقيت الحقول البيضاء واضحة. وبعد التبديل إلى الوضع الفاتح بقيت لوحة النموذج، الصورة، العناوين، والحقول متناسقة مع الهوية الأصلية، ولم يظهر انكسار واضح.

في صفحة `/about` كشفت قياسات الصور أن صورة الـHero كانت بعرض سبعة أعمدة فعلياً، بينما حاوية النص بدأت من العمود 8 بعرض خمسة أعمدة مع صورة معلنة بثمانية أعمدة؛ أدى ذلك إلى تداخل شبكي وظهور فراغ أبيض كبير في الجولة المرئية. تم تعديل الصورة إلى `md:col-span-7` لتتوافق مع حاوية النص وتمنع التداخل دون تغيير الألوان أو الصور أو التخطيط التحريري المقصود.

أظهر فحص CSS الفعلي أن الشبكة كانت 12 عموداً بعرض viewport فعلي 1280px، لكن الصورة بقيت في `grid-row` ثانية رغم توافق الأعمدة؛ لذلك ظهر الفراغ أعلى الصفحة. تم تثبيت الصورة على `md:row-start-1` لتشارك صف النص نفسه، وهو إصلاح تخطيطي لا يبدل الهوية البصرية.

بعد إعادة البناء وتشغيل آخر build، ظهرت صورة Hero في صفحة `/about` في الصف الأول بجانب كتلة النص، واختفى الفراغ الأبيض الكبير. بقيت الصورة الوردية، العنوان، النص، divider، والتذييل ضمن الهوية الأصلية.

## دورة QA الجديدة — fixtures والحركة

- أضيفت fixtures تطويرية مشروطة بـ `import.meta.env.DEV` و`?qa=fixtures` فقط؛ لا تُقرأ في البناء الإنتاجي ولا تُكتب إلى Supabase.
- ظهرت ثلاث بطاقات اختبار مختلفة: منتج بسعر، منتج ثانٍ بسعر، ومنتج `custom_only` بسعر عند الطلب.
- مرشح `Baskets` بدّل القائمة إلى بطاقة واحدة كما هو متوقع.
- سنة التذييل بقيت 2026، وشريط مناطق التوصيل ظهر دون تغيير الهوية.
- مرشح `Accessories` بدّل القائمة إلى `Soft Blida Scarf` فقط، ومرشح `Home` بدّلها إلى `Custom Rose Cushion` فقط؛ لم تظهر أخطاء أو تحميل مستمر.

- بعد إصلاح رابط fixture، فتح `/product/qa-rose-basket?qa=fixtures` صفحة التفاصيل بنجاح، وظهرت الصورة، السعر، اللونان، المقاسان، الوصف، والتذييل 2026.
- تم تغيير المقاس من `Small` إلى `Medium` وإدخال ملاحظة اختبار في الحقل دون خطأ ظاهر. لم يتم إرسال طلب حقيقي أو تعديل قاعدة بيانات Supabase.
- أضيف fixture إلى السلة بنجاح، وظهرت بطاقة `Rose Garden Basket` بالمقاس `Medium` والسعر 2,400 DZD، مع أزرار زيادة/خفض/حذف وحقول بيانات الطلب. ظهر تنبيه عدم تفعيل قناة تواصل الأدمن كما هو متوقع، ولم يتم إرسال الطلب.
- رفعت كمية fixture إلى 2 فارتفع الإجمالي إلى 4,800 DZD، ثم خفّضتها إلى 1 فعاد الإجمالي إلى 2,400 DZD. ظهرت إشعارات التوصيل وإغلاقها دون أخطاء ظاهرة.
- حُذف fixture من السلة بنجاح وعادت الحالة الفارغة مع رابط `Browse the shop`. فتح الرابط `/shop` وأظهر حالة `No products yet` دون حلقة تحميل أو خطأ ظاهر؛ بيانات المتجر المنشورة بقيت دون إضافة منتجات اختبارية.
- أُعيد فتح `/` بعد إصلاحات الجولة الثانية؛ ظهرت أقسام Hero والمزايا والقصة والتذييل وسنة 2026. سجل المتصفح لا يحتوي أخطاء تشغيل؛ الظاهر فقط تحذيرا React Router المعروفان حول أعلام التوافق المستقبلية `v7_startTransition` و`v7_relativeSplatPath`.
- تحمّلت `/shipping` وأكدت نصياً ومرئياً دعم التوصيل إلى ولايتي البليدة والجزائر العاصمة، مع بطاقتي المناطق وتذييل 2026.
- تحمّلت `/faq` وظهرت الأسئلة الأربعة وإجاباتها ومعلومة التوصيل، دون خطأ ظاهر أو انكسار في التخطيط.
- فُتح `/admin/login` محلياً، وتم تسجيل الدخول بنجاح بالجلسة المصرح بها. ظهرت لوحة الأدمن ورابط سجل الأمان دون كشف بيانات الدخول في الواجهة العامة.
- فُتح `/admin/security-log` بعد الدخول؛ ظهر السجل من Supabase وفيه محاولة الدخول الناجحة الحالية، ومحاولات فتح أدمن غير مصرح بها سابقة، وتسجيل الخروج. الجدول يوضح أن التتبع محصور بمحاولات ومسارات وأنشطة الأدمن ولا يتتبع زوار المتجر العام.
- بعد تسجيل الخروج، عُدّلت لغة `/` من الإنجليزية إلى العربية؛ ظهرت الترجمات العربية كاملة، وتحوّل التخطيط إلى RTL بصورة سليمة، وبقيت الألوان والتذييل ومعلومة التوصيل متناسقة.
- في الصفحة الرئيسية العربية، فُعّل الوضع الداكن ثم أُعيدت اللغة إلى الإنجليزية؛ بقي النص الرئيسي والعناوين والأزرار مقروءة، وظهر زر `light_mode` للعودة، ولم يظهر انكسار واضح في التخطيط أو تبدل غير مقصود للهوية.
- أُعيد بناء الإنتاج بعد إضافة انتقال مسار قصير، حركة Hero منخفضة السعة، skeleton تحميل للمنتجات، stagger للبطاقات، Reveal لأقسام About، وحركة فتح/إغلاق قائمة الجوال. نجح `npm run build` دون أخطاء؛ ظهر فقط تحذير حجم chunk القائم.
- فُتح `/shop?qa=fixtures` بعد البناء، فظهرت البطاقات الثلاث والتصنيفات والتذييل 2026 دون انكسار. وفُتح `/about`، فظهرت صورة Hero بجانب النص، القسمان، بطاقات الثقة، وأزرار CTA مع بقاء الهوية والألوان الحالية.
- نُشرت النسخة النهائية من `/home/ubuntu/crochetbyalae-latest/dist` إلى موقع Netlify الرئيسي باستخدام site ID الرسمي `2636709b-049e-4481-b5ea-066d88f9b409`، ونجح النشر بالمعرّف `6a7cfe2aaedbcc57c267eac5`.
- فُتح `https://crochetbyalae.netlify.app/` بعد النشر؛ بعد اكتمال انتقال المسار ظهرت الصفحة الرئيسية العربية، Hero، شريط التوصيل، الروابط العامة، وتذييل 2026. لقطة الفتح الأولى كانت أثناء انتقال الدخول فبدت بعض العناصر غير مكتملة مؤقتاً، ثم اكتمل الظهور عند إعادة العرض دون استمرار blank أو خطأ JavaScript.
- فُتح `/shop` على الموقع المنشور؛ ظهرت skeleton أثناء التحميل الأول ثم اختفت بعد اكتمال الطلب، وانتهى المسار إلى الحالة الفارغة `لا توجد منتجات بعد` مع التذييل 2026. لم تظهر fixtures أو حلقة تحميل مستمرة.
- عند فتح `https://crochetbyalae.netlify.app/admin` دون جلسة، تم التحويل إلى `/admin/login`. بعد إدخال بيانات الأدمن المصرح بها وإرسال النموذج، انتقلت الجلسة إلى `/admin` وظهرت لوحة التحكم، الإحصاءات، وروابط سجل الأمان والمنتجات والطلبات. لم تُعرض بيانات الدخول في الواجهة.
- فُتح `/admin/security-log` على الموقع المنشور بعد الدخول؛ ظهر سجل Supabase وبداخله دخول ناجح حديث، محاولات فتح مرفوضة لمسار الأدمن، وتسجيلات خروج. يعرض الجدول الوقت والبريد والإجراء والتفاصيل والمؤشر التقني، مع بقاء الوصف محصوراً في نشاط الأدمن وعدم تتبع زوار المتجر العام.
- بقيت صفحة سجل الأمان ظاهرة بعد محاولة الضغط الأولى على زر الخروج، لذلك سيُعاد اختبار الخروج من موضع الزر المرئي مباشرة قبل إنهاء الفحص الأونلاين.
- بعد النقر المضبوط على زر الخروج عبر عنصره الفعلي، أُغلقت الجلسة وأُعيد التوجيه إلى `https://crochetbyalae.netlify.app/admin/login`. ظهرت صفحة الدخول فقط، وبذلك لا تبقى لوحة الأدمن متاحة بعد تسجيل الخروج.
