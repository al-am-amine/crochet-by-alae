# تحليل شعار Crochet by Alae

## المصدر

رابط الشعار المحفوظ في Supabase Storage:

`https://ikgxaxanhxgnsadkawuz.supabase.co/storage/v1/object/public/product-images/logo/bb5f2307-8df2-49aa-b77a-26066e55a60c.png`

## الملاحظات البصرية

الملف صورة PNG مربعة بأبعاد 947×947 بكسل. يظهر فيها ختم دائري وردي لامع مع حرفي `AA` بالأسود في المنتصف، وتوجد مساحة فاتحة حول الدائرة. لذلك استُخدم كعلامة دائرية صغيرة في الهيدر والشريط الجانبي وصفحات دخول الأدمن، مع `object-cover` وحافة خفيفة، بدلاً من وضعه داخل مستطيل أو عرضه كصورة مكسورة.

## سبب عدم الظهور السابق

كانت قيمة `site_settings.logo_url` محفوظة فعلاً، لكن الهيدر والتذييل ولوحات الأدمن كانت تعرض نصاً أو رمز `storefront` ثابتاً ولم تكن تستهلك `logo_url`.

## قرار الدمج

أُضيف سياق مشترك يقرأ `logo_url` مع قيمة احتياطية، ومكوّن `BrandLogo` لإعادة الاستخدام، وتحديث تلقائي عند رفع شعار جديد. كما رُبط الشعار بالـfavicon ووسوم `og:image` و`twitter:image`، واستُبدلت رموز التواصل في التذييل بمكوّن `ChannelIcon` المرسوم.
